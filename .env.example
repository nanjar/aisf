# Used by docker-compose to bake the API URL into the frontend build.
# Must be the PUBLIC URL the browser will call (not localhost/Tailscale-only).
NEXT_PUBLIC_API_URL=https://asf-api.nanjarbudiman.com
